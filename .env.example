# PostgreSQL database connection settings
POSTGRES_USER=mathportal
POSTGRES_PASSWORD=mathportal
POSTGRES_DB=mathportal
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}
